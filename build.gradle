plugins {
    id 'java'
}

def javaProjects = [
        project(":module-1"),
        project(":module-2"),
        project(":module-3")
]

configure(javaProjects) {
    apply plugin: 'java'
    version '1.0'
    repositories {
        mavenCentral()
    }

    dependencies {

    }

    test {
        useJUnitPlatform()
    }
}